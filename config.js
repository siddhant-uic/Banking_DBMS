const config = {
    db: {
        /* don't expose password or any sensitive info, done only for demo */
        host: "localhost",
        user: "root",
        password: "yuvrajsingh42",
        database: "bankingsystem",
    },
};
module.exports = config;